// Get the first argument from process.argv (index 2)
const arg1 = process.argv[2];

// Get the second argument from process.argv (index 3)
const arg2 = process.argv[3];

// Print the arguments in the specified format.
// If an argument is undefined (not provided), it will print "undefined" as per the example.
console.log(`${arg1} is ${arg2}`);
